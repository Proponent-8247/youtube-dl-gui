# End-to-end review checklist — continuation 5

This file continues the prior end-to-end audit checklists. Findings are committed individually for interruption-safe recovery. Production source is unchanged in this review pass.

- [ ] **O032 — Downloader progress percentages are parsed but not bounded before assigning WinForms progress values**
  - **Priority / state:** Medium / live-output robustness; SOURCE-CONFIRMED residual coverage gap.
  - **Finding:** `DownloadHelper.GetTransferData` uses invariant `float.TryParse` and returns parsed progress through a `ref float`, but it does not constrain the value to the UI's valid range. Both `frmDownloader` and `frmExtendedDownloader` subsequently execute `pbStatus.Value = (int)Math.Floor(Percentage)` on their UI thread while the progress bar uses the normal 0–100 range. Thus a syntactically valid provider line such as `[download] 101% ...` (or a much larger numeric percentage) passes the parser but can produce `ArgumentOutOfRangeException` when assigned. In quick download that exception propagates back through `Control.Invoke` into the worker's broad catch, which marks the operation `ProgramError` and terminates the child; extended paths have the same unbounded assignment pattern. The existing `Progress.MalformedRowsCannotThrow` regression exercises `GetTransferData` in isolation and therefore does not cover this form-level sink.
  - **Evidence:** `youtube-dl-gui/Classes/DownloadHelper.cs`, `GetTransferData`, assigns `Percentage = ParsedPercentage` with no range check. `youtube-dl-gui/Forms/frmDownloader.cs` around the progress-processing loop assigns `(int)Math.Floor(Percentage)` to `pbStatus.Value`; `frmExtendedDownloader.cs` does the same in both single/batch processing loops. `ExtendedProgressBar` inherits WinForms ProgressBar range semantics.
  - **Audit rationale checkpoint:** Progress text from a child process is diagnostic/status input, not authority to break the transfer. Preserve the textual percentage if useful, but normalize/clamp or reject the numeric UI value before assigning it. Do not treat one malformed progress line as a downloader execution failure when the child can continue normally.
  - **Acceptance for patch pass:** Drive the actual quick and extended forms with a controlled provider emitting ordinary 0/0.1/99.9/100 percentages plus 101, huge finite values, negative, NaN/Infinity text, missing `%`, and culture-sensitive variants. The UI must never assign outside its valid range or change a successful child exit into ProgramError solely because of progress text. Valid ordinary progress display/ETA must remain unchanged; malformed lines should be logged/ignored or safely represented according to a documented policy.

- [ ] **O033 — Batch Downloader arbitrary-file import/load actions let ordinary file I/O failures escape UI events**
  - **Priority / state:** Low-to-Medium / file-import fault containment; SOURCE-CONFIRMED.
  - **Finding:** The shipping Batch Downloader has two user-selected arbitrary-file paths that are read after an `OpenFileDialog` without a containing I/O boundary. `mBatchDownloaderLoadArgsFromFile_Click` calls `File.ReadAllText(ofd.FileName)` after only an existence check. `mBatchDownloaderImportLinksFromFile_Click` constructs `StreamReader(ofd.FileName)` and reads line-by-line with no catch. A selected file can be deleted, replaced by a directory, locked, lose ACL access, or encounter an I/O failure after the dialog and before/during the read; those failures escape the WinForms event to global exception handling rather than becoming a contained import/load failure. This is distinct from O025, which tracks the application's special `args.txt` persistence mode, and O024, which tracks the main-form raw batch-from-file worker.
  - **Evidence:** `youtube-dl-gui/Forms/frmBatchDownloader.cs`, `mBatchDownloaderLoadArgsFromFile_Click` performs `File.ReadAllText` on the chosen file with no `try/catch`; `mBatchDownloaderImportLinksFromFile_Click` creates a `StreamReader` for the chosen path and loops on `ReadLine()` with no I/O exception handling. Both are ordinary shipping menu actions.
  - **Audit rationale checkpoint:** File-dialog selection does not guarantee the file remains readable afterward. Import failures should be operation-local and should preserve the current batch/custom-argument state unless a clear partial-import policy is intentionally chosen. Do not replace the current text-file workflows or silently drop lines merely to avoid exceptions.
  - **Acceptance for patch pass:** For both actions, test a normal Unicode file, empty file, deleted-after-dialog file, locked file, access-denied file, directory substituted for the selected path, and a controlled mid-read I/O failure where feasible. No global/unhandled exception may escape. A failed argument-file load must preserve the previous argument text; a failed link import must have an explicit all-or-partial commit policy and must not duplicate or corrupt already queued links. Normal ordering, blank-line handling, Unicode, long lines within supported limits, and existing menu behavior must remain unchanged.

- [ ] **O034 — Live output reader callbacks still read WinForms progress controls off the UI thread**
  - **Priority / state:** Low-to-Medium / UI thread-affinity and output robustness; SOURCE-CONFIRMED.
  - **Finding:** `BoundedProcessOutput` invokes its `OutputDataReceived` subscribers on background reader tasks. The repaired download forms marshal most **writes** through `Control.Invoke`, but their callbacks still evaluate `pbStatus.Style`, `pbStatus.Text`, and `pbStatus.Value` directly on the reader task to decide whether an update is needed. WinForms controls are not generally thread-safe for arbitrary property access; only the documented marshaling methods and a small set of methods are safe cross-thread. With illegal cross-thread checking enabled this pattern can throw; with it disabled it remains an unsupported race against disposal/handle recreation. The existing concurrent-output close stress runs on a Release-style environment where illegal cross-thread checks are normally disabled, so a green stress run does not prove thread-affinity correctness.
  - **Evidence:** `youtube-dl-gui/Forms/frmDownloader.cs`, the `BoundedProcessOutput.OutputDataReceived` callback directly checks `pbStatus.Style != ...`, `pbStatus.Text != ...`, and `pbStatus.Value != 0` before invoking setters. `youtube-dl-gui/Forms/frmExtendedDownloader.cs` single-download callback does the same, and its batch processing loop also reads `pbStatus.Style` on the worker before invoking the setter. `Controls/BoundedProcessOutput.cs` runs output reading and callback invocation on task-pool reader tasks rather than the form's synchronization context.
  - **Audit rationale checkpoint:** Avoiding redundant UI writes is not worth violating the control's thread-affinity contract. Read-and-update the control state inside a single marshaled UI delegate, or keep non-control model state on the worker. This is distinct from historical output-message synchronization and O032's out-of-range numeric value: O034 is about which thread accesses the control at all.
  - **Acceptance for patch pass:** Set `Control.CheckForIllegalCrossThreadCalls = true` in a dedicated regression and drive real quick/extended forms with ordinary, noisy, status-transition, and concurrent stdout/stderr fixtures. No cross-thread control access may be detected, including during close/disposal and handle recreation. Verify status transitions still avoid unnecessary flicker, cancellation still terminates owned children, and existing close/output stress tests continue to pass in both default and x86 harnesses.
