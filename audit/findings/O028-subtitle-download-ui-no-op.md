- [x] **W251 / O028 — WITHDRAWN FALSE POSITIVE: subtitle download no-op is Release-hidden**
  - **State:** Withdrawn after broader form-construction review; published history intentionally preserved.
  - **Correction:** `frmSubtitles.btnSubtitlesDownload_Click` is indeed empty, but `frmMain` explicitly executes `mDownloadSubtitles.Enabled = mDownloadSubtitles.Visible = false` whenever `Program.DebugMode` is false. The same Release branch removes the Debug tab and disposes the extended-converter debug button. Therefore the unfinished subtitle form is not exposed as a normal shipping Release feature, contrary to the original O028 wording.
  - **Evidence:** `youtube-dl-gui/Forms/frmMain.cs`, constructor: in the non-Debug branch, `mDownloadSubtitles.Enabled = mDownloadSubtitles.Visible = false;`. `Program.DebugMode` is set true only inside `#if DEBUG`; normal Release builds take the hiding branch. `Forms/frmSubtitles.cs` remains unfinished development code.
  - **Audit rationale checkpoint:** This belongs under the existing experimental/unfinished-tool policy (P006/V010), not as a current Release correctness defect. The empty handler can remain an implementation gap to address only if/when the feature is intentionally promoted. Do not patch production behavior solely to satisfy the withdrawn finding.
  - **Future acceptance if feature is promoted:** Before making the menu Release-visible, implement and test the subtitle workflow end to end (languages, automatic/manual subtitles, provider failure, cancellation, output placement) or retain the explicit Release hide. Add a UI reachability test so Debug-only tools cannot accidentally become shipping no-ops.
